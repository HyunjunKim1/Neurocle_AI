<UserControl x:Class="HDSInspector_AI.GUI.UserControls.Main.GridLeft.Uc_HW"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="384" Background="#121d2f"
             Loaded="UserControl_Loaded" Unloaded="UserControl_Unloaded">

    <!-- Control Resource -->
    <UserControl.Resources>
        <Style x:Key="HardwareTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#D9E2F0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="HardwareValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#4FC3F7"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="HardwareDetailStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#AAB7C8"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        
        <Style x:Key="HardwareProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Height" Value="12"/>
            <Setter Property="Minimum" Value="0"/>
            <Setter Property="Maximum" Value="100"/>
            <Setter Property="Background" Value="#26364D"/>
            <Setter Property="Foreground" Value="#29B6F6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" CornerRadius="6"/>
                            <Grid x:Name="PART_Track" ClipToBounds="True">
                                <Border x:Name="PART_Indicator" HorizontalAlignment="Left" Background="{TemplateBinding Foreground}" CornerRadius="6"/>
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- GUI -->
    <Border BorderBrush="WhiteSmoke" BorderThickness="2" CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="10" ShadowDepth="1"/>
        </Border.Effect>
        <Border Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1.25*"/>
                    <RowDefinition Height="1.25*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <!-- 제목 -->
                    <TextBlock Grid.Row="0" Text="Hardware Status" Foreground="White" FontWeight="Bold" FontSize="15" VerticalAlignment="Top"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                        <Ellipse x:Name="ellStatus" Width="8" Height="8" Fill="#29B6F6" Margin="0, 4, 6, 0"/>
                        <TextBlock x:Name="tbkStatus" Text="Ready" FontSize="10" VerticalAlignment="Top"/>
                    </StackPanel>
                </Grid>

                <!-- CPU -->
                <Grid Grid.Row="1" Margin="0,3,0,4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="24"/>
                        <RowDefinition Height="14"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="90"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="CPU" Style="{StaticResource HardwareTitleStyle}"/>
                        <TextBlock x:Name="tbkCpuUsage" Grid.Column="1" Text="0.0%" Style="{StaticResource HardwareValueStyle}"/>
                    </Grid>
                    <ProgressBar x:Name="pBarCpuUsage" Grid.Row="1" Value="0" Style="{StaticResource HardwareProgressBarStyle}"/>
                </Grid>

                <!-- GPU -->
                <Grid Grid.Row="2" Margin="0,3,0,4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="24"/>
                        <RowDefinition Height="18"/>
                        <RowDefinition Height="14"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="90"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="GPU" Style="{StaticResource HardwareTitleStyle}"/>
                        <TextBlock x:Name="tbkGpuUsage" Grid.Column="1" Text="N/A" Style="{StaticResource HardwareValueStyle}"/>
                    </Grid>
                    <TextBlock x:Name="tbkGpuMemory" Grid.Row="1" Text="VRAM 0.0 GB / 0.0 GB" Style="{StaticResource HardwareDetailStyle}"/>
                    <ProgressBar x:Name="pBarGpuUsage" Grid.Row="2" Value="0" Style="{StaticResource HardwareProgressBarStyle}"/>
                </Grid>

                <!-- Drive -->
                <Grid Grid.Row="3" Margin="0,3,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="24"/>
                        <RowDefinition Height="18"/>
                        <RowDefinition Height="14"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="90"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="E Drive" Style="{StaticResource HardwareTitleStyle}"/>
                        <TextBlock x:Name="tbkDriveUsage" Grid.Column="1" Text="0.0 %" Style="{StaticResource HardwareValueStyle}"/>
                    </Grid>
                    <TextBlock x:Name="tbkDriveCapacity" Grid.Row="1" Text="Free 0 GB / Total 0 GB" Style="{StaticResource HardwareDetailStyle}"/>
                    <ProgressBar x:Name="pBarDriveUsage" Grid.Row="2" Value="0" Style="{StaticResource HardwareProgressBarStyle}"/>
                </Grid>
            </Grid>
        </Border>
    </Border>
</UserControl>
